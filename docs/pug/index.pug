doctype html
html(lang="en-US")
    
    head
        meta(charset="utf-8")
        title Materialize Bootbox
        link(rel="stylesheet" href="css/materialize.min.css")
        link(rel="stylesheet" href="css/prism.css")
        link(rel="stylesheet" href="css/nouislider.css")
        style.
            .brand-logo{ padding-left: 15px !important; }
        script.
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)})(window,document,'script','//www.google-analytics.com/analytics.js','ga');ga('create','UA-109160705-1','auto',{'allowLinker':true});ga('require','linker');ga('linker:autoLink', ["iqbalfn.com"]);ga('send','pageview');
    body
        nav.nav-extended
            .nav-wrapper
                a.brand-logo(href="#") Materialize Bootbox v1.0.0
                a.sidenav-trigger(href="#" data-target="mobile-demo")
                    i.material-icons menu
                ul#nav-mobile.right.hide-on-med-and-down
                    li
                        a(href="https://github.com/iqbalfn/materialize-bootbox") Github
                ul#mobile-demo.sidenav
                    li
                        a(href="https://github.com/iqbalfn/materialize-bootbox") Github
            .nav-content
                include tabs.pug
        
        .container
            .row
                .col.s12
                    include tab-getting-started.pug
                    include tab-alert.pug
                    include tab-confirm.pug
                    include tab-prompt.pug
                    include tab-dialog.pug
                    include tab-options.pug
                    include tab-methods.pug
                    include tab-box.pug
        
        script(src="js/materialize.min.js")
        script(src="js/nouislider.min.js")
        script(src="js/mzbox.min.js")
        script(src="js/prism.js" data-manual)
        script.
            new M.Tabs(document.querySelector('.tabs'));
            
            function ds(str){
                return str.replace(/&gt;/g, '>').replace(/&lt;/g, '<');
            }
            
            document.querySelectorAll('.card').forEach(e => {
              let btn = e.querySelector('.card-action > a');
              let src = e.querySelector('.language-js');
              
              if(!btn || !src)
                  return;
             
              let code = src.innerHTML;
              btn.onclick = function(){
                  try{ eval(code) }catch(e){}
                  return false;
              }
            });
            
            Prism.highlightAll();